--- src/tModLoader/Terraria/Graphics/Light/TileLightScanner.cs
+++ src/Terraclient/Terraria/Graphics/Light/TileLightScanner.cs
@@ -5,6 +_,7 @@
 using Terraria.ID;
 using Terraria.Utilities;
 using Terraria.ModLoader;
+using Terraria.Terraclient.Implementation.Defaults;
 
 namespace Terraria.Graphics.Light
 {
@@ -179,6 +_,18 @@
 					num2 = 0.3f;
 					break;
 			}
+
+			// Is putting our stuff *before* ModifyLight a good idea? Maybe...
+			// Or we could make it configurable?
+
+			if (ModContent.GetCheatWithFallback<FullBrightCheat>().IsEnabled) {
+				float lightLevel = ModContent.GetCheatWithFallback<FullBrightDimOptionCheat>().IsEnabled ? 0.5f : 1f;
+				bool shouldAddLight = ModContent.GetCheatWithFallback<FullBrightLightQualityOptionCheat>().IsEnabled || (x % 2 == 0 && y % 2 == 0);
+
+				if (shouldAddLight)
+					num = num2 = num3 = lightLevel;
+			}
+
 			WallLoader.ModifyLight(x, y, tile.wall, ref num, ref num2, ref num3);
 
 			if (lightColor.X < num)
